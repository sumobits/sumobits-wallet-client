/**
 * @format
 */
import React from 'react';
import {
	ScrollView,
	StyleSheet,
	Text,
	View,
} from 'react-native';
import {
	Colors,
	Fonts,
} from '../../utils';

const TransferView = () => {
	return (
		<ScrollView>
			<Text>Transfers</Text>
		</ScrollView>
	);
};

export default TransferView;
