/**
 * @format
 */
import React, {
	useContext,
	useState
} from 'react';
import { 
	ScrollView,
	Text,
} from 'react-native';

const PreferencesView = () => {
	return (
		<ScrollView>
			<Text>Preferences</Text>
		</ScrollView>
	);
};

export default PreferencesView;
